# Compiled C FFI artifacts (rebuilt via c/Makefile; sources are tracked)
c/*.so
c/sqlite3.o

# Runtime data (sqlite db + wal/shm)
data/

# Compiled asset pipeline output (adr/0025): content-hashed copies +
# gzip siblings + the manifest, regenerated by compile_assets/0 on every
# start. assets/ (the sources) IS committed; public/ never is.
public/

# Backup files
*.bak

# Temp artifacts
/tmp/
*.tmp

# Compiled Prolog QLF files
*.qlf

# Note: vendor/ IS committed (vendoring philosophy, see adr/0003).
